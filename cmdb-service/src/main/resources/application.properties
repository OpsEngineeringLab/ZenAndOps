# ZenAndOps CMDB Service Configuration

# HTTP
quarkus.http.port=8083

# MongoDB
quarkus.mongodb.connection-string=mongodb://localhost:27017
quarkus.mongodb.database=zenandops-cmdb
quarkus.mongodb.metrics.enabled=true
quarkus.mongodb.connect-timeout=5s
quarkus.mongodb.server-selection-timeout=5s
quarkus.mongodb.read-timeout=10s

# OIDC token validation (Keycloak)
quarkus.oidc.auth-server-url=http://keycloak:8180/realms/zenandops
quarkus.oidc.client-id=zenandops-backend
quarkus.oidc.tls.verification=none
quarkus.oidc.token.issuer=http://localhost:${KEYCLOAK_EXTERNAL_PORT:8180}/realms/zenandops

# CMDB Configuration
cmdb.impact-analysis.max-depth=${CMDB_IMPACT_ANALYSIS_MAX_DEPTH:10}

# OpenAPI and Swagger UI
quarkus.smallrye-openapi.info-title=ZenAndOps CMDB Service API
quarkus.smallrye-openapi.info-version=1.8.2-SNAPSHOT
quarkus.swagger-ui.always-include=true

# Kafka
kafka.bootstrap.servers=localhost:9092
mp.messaging.outgoing.cmdb-events.connector=smallrye-kafka
mp.messaging.outgoing.cmdb-events.topic=cmdb-events
mp.messaging.outgoing.cmdb-events.value.serializer=org.apache.kafka.common.serialization.StringSerializer

# OpenTelemetry core — traces only, sent directly to Tempo
quarkus.otel.exporter.otlp.endpoint=${OTEL_EXPORTER_OTLP_ENDPOINT:http://localhost:4317}
quarkus.application.name=zenandops-cmdb

# Tracing (OTLP → Tempo)
quarkus.otel.traces.enabled=true
quarkus.otel.traces.sampler=parentbased_traceidratio
quarkus.otel.traces.sampler.arg=${OTEL_TRACES_SAMPLER_ARG:1.0}

# Disable OTel logs and metrics (logs via Fluent Bit → Loki, metrics via Prometheus scrape)
quarkus.otel.logs.enabled=false
quarkus.otel.metrics.enabled=false

# Metrics via Micrometer Prometheus registry (scraped by Prometheus at /q/metrics)
quarkus.micrometer.binder.jvm=true
quarkus.micrometer.binder.http-server.enabled=true
quarkus.micrometer.binder.http-server.match-patterns=/api/v1/cmdb/organizations=/api/v1/cmdb/organizations,/api/v1/cmdb/services=/api/v1/cmdb/services,/api/v1/cmdb/assets=/api/v1/cmdb/assets,/api/v1/cmdb/cis=/api/v1/cmdb/cis

# Enable histogram buckets for HTTP server metrics
quarkus.micrometer.distribution.percentiles-histogram[http.server.requests]=true
quarkus.micrometer.distribution.minimum-expected-value[http.server.requests]=0.001
quarkus.micrometer.distribution.maximum-expected-value[http.server.requests]=5

# Structured JSON logging
quarkus.log.console.json=true
